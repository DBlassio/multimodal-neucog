numpy>=1.24
pandas>=2.0
openpyxl>=3.1          # .xlsx reading (psychometric data)
scipy>=1.11            # statistical tests
mne>=1.6               # .set reading, preprocessing, topomaps
scikit-learn>=1.3      # SVM, Random Forest, cross-validation
torch>=2.1             # lightweight MLP + gating module
shap>=0.44             # SHAP values → topomap projection
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66             # progress bars (EEG loading loop)
joblib>=1.3            # parallelism (used internally by sklearn)
jupyter>=1.0
ipykernel>=6.0

#https://nemar-dev.ucsd.edu/dataexplorer/detail?dataset_id=ds006260